# Copy this file to .env
# Your NVIDIA NIM API key (server-side only, never exposed to browser)
NVIDIA_API_KEY=your_nvidia_api_key_here

# Your Google Gemini API key (client-side, via VITE_ prefix)
VITE_GEMINI_API_KEY=your_gemini_api_key_here
